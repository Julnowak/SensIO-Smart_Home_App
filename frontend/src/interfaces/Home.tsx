interface Home {
    home_id: number,
    name: string,
    address: string,
    regards: string,
    owner: string,
    floor_num: string,
    code: string,
    current: boolean,
    created_at: string
}